<p>
  &#x2326; <strong>Receita deste mês:</strong>
  R$<%= @income_current %> | &#x2326; <strong>Você economizou:</strong>
  R$<%= @difference_month_ago%> |
  <%= link_to 'Buscar por data', reports_index_path %>
</p>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Descrição</th>
      <th>Valor</th>
    </tr>
  </thead>

  <tbody>
    <% @expense_current_month.each do |expense| %>
      <tr class="active">
        <td><%= link_to expense.description, expense %></td>
        <td><%= expense.value %></td>
      </tr>
    <% end %>
    <i>Gastos rescentes</i>
  </tbody>
</table>

  <p class="bg-danger">
    <strong>Total geral:</strong>
      R$<%= @total %> |
    <strong>Debito em cartão:</strong>
      R$<%= @total_card %>
    <div class='receita'>
     <strong>Valor em caixa:</strong>
      R$<%= @difference_current %> &#x232B;
    </div>
  </p>
<div>
  <%= link_to t('actions.add'), new_expense_path, class: "btn btn-default btn-lg" %>
  <%= paginate @expense_current_month, theme: 'twitter-bootstrap-3' %>
</div>
