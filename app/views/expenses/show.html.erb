<p>
  <strong>Valor:</strong>
  <%= @expense.value %>
</p>

<p>
  <strong>Descrição:</strong>
  <%= @expense.description %>
</p>

<p>
  <strong>Usuario:</strong>
  <%= @expense.user.email %>
</p>

<p>
  <strong>Data:</strong>
  <%= @expense.created_at.strftime("%d %b. %Y") %>
</p>

<%= link_to 'actions.back', root_path %>|
<%= link_to 'actions.edit', edit_expense_path %>|
<%= link_to 'actions.delete', expense_path(@expense), method: :delele,
   data: { confirm: 'Deseja excluir?' } %>

