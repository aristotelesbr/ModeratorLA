<% if record.errors.any? %>
  <div class='error-messages'>
    <p>
      'Há erros no formulario, verifique antes de continuar.'
    </p>

    <ul>
      <% for message in record.errors.full_messages %>
        <li>
          <%= message %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>
